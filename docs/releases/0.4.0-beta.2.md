# HackRFPPH2 0.4.0-beta.2

## What's new

- Added donation popup at app start and on app close flow.
- Popup is always dismissible; app use remains unrestricted.
- Added donation addresses in app UI and README.
- Added Reddit-ready UI screenshots in `docs/reddit/`.
- Added debug screenshot helper mode for connected-state visual capture.

## APK artifacts

- `app-free-debug.apk` (installable debug build)
- `app-free-release-unsigned.apk` (optimized unsigned release build)
- `SHA256SUMS.txt`
